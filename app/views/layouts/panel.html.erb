<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Promoaki</title>
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'ad', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="hold-transition skin-black sidebar-mini">
    <div class="wrapper">
      <%= render 'panel/elements/header' %>
      <%= render 'panel/elements/sidebar' %>

      <div class="content-wrapper">
        <div class="row">
          <div class="col-md-12">
            <% if notice || alert %>
              <div class="content-header">
                <div class="top-buffer alert <%= notice ? "alert-success" : "" %><%= alert ? "alert-danger" : "" %> alert-dismissible" role="alert">
                  <button type="button" class="close" data-dismiss="alert">
                    <span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                  <div >
                    <% if notice %>
                        <%= notice %>
                    <% elsif alert %>
                        <%= alert %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>

        <%= yield %>
      </div>

      <%= render 'panel/elements/footer' %>
      <%= render 'panel/elements/aside' %>
      <!-- Add the sidebar's background. This div must be placed
           immediately after the control sidebar -->
      <div class="control-sidebar-bg"></div>
    </div>
    <%= javascript_include_tag 'ad', 'data-turbolinks-track': 'reload' %>
  </body>
</html>
